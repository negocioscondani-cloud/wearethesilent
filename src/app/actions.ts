'use server';

// This file is intentionally left almost empty.
// The previous server action `submitApplication` was using the Firebase Admin SDK
// and causing build failures because server environment variables were not
// available in the build environment. The function did not appear to be in use.
// Keeping the 'use server' directive for potential future server actions.
